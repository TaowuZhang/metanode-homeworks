# 已迁移：状态锁相流

> 旧名：Project 1 字段同步  
> 新名：状态锁相流

本文件保留为兼容入口，避免旧链接断裂。当前主源已迁移到：

```plain text
docs/ops/status-phase-lock-from-labels.md
```

兼容脚本仍可运行：

```bash
bash scripts/sync-project1-fields-from-labels.sh
```

但推荐使用新脚本：

```bash
bash scripts/status-phase-lock-from-labels.sh
```
