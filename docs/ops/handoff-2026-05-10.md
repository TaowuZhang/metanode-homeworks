# 交接：2026-05-10 凌晨收束

这份交接给明天的克 / 谷使用。它不是新计划，不扩张系统；只把今天已经沉地的东西、明天应该先看的位置、下一刀的边界说清楚。

## 当前状态

- `main` 已推到 `origin/main`。
- 终端桥已经改成「多车道并行 / 批次通行权」模式：
  - 写终端前先看 Notion「终端」页顶部车道面板。
  - 车道只用于分流和回看边界，不是权利、占据或排他。
  - 所有车道都能写；多个车道可以同时行进。
  - 继续时只读自己车道本轮写过的终端，不扫全场。
- 墨墨方向已经从想法落到最小只读工具：
  - `cargo run -- weigh`
  - 底层调用 `scripts/maimemo-probe.mjs`
  - token 优先读 `MAIMEMO_TOKEN`，否则读 macOS Keychain 的 `maimemo-open-api`
  - 输出今日进度、首次反馈、未来复习负债、称重建议、卡词候选。
- CLI 命名债已经处理一轮：
  - `strata` 已迁移为动词命令 `surface`，不保留名词 alias。
  - `weigh` 已作为墨墨记忆天气 / 认知负荷入口。
  - `palm` 暂时保留：它可作动词，但仍需观察是否会把命令感带回名词模块。
- 分发管线已经补了发布包层：
  - `node scripts/wo-excrete.mjs` 会为每条 `type=excrete` 事件生成 `ai-browse/out/packages/<event-id>.json` 与 `.md`。
  - 发布包 smoke 已用临时目录验证成功，真实仓库未被污染。
  - `runner` 会把 `packageJson` / `packageMd` 写进外手日志。
- Web 第零期已经完成一轮闭环：
  - NNGroup 复审、Apple Design 复审、文案修整、证据来路、封板说明都已沉淀到 `docs/ops/`。
  - `web/content/*.json` 可解析。
  - 第零期来路链接已修到 GitHub 路径渲染。
- Notion 公共入口与个人展示边界已有草案：
  - `docs/ops/notion-public-entry.md`
  - `docs/ops/personal-display-and-web.md`

## 明天先看哪里

建议顺序：

1. `git log --oneline --decorate -12`
2. `docs/ops/handoff-2026-05-10.md`
3. `docs/ideas/maimemo-memory-roots.md`
4. `docs/ops/distribution-map.md`
5. `docs/ops/publishing-package.md`
6. `docs/ops/web-issue-zero-release-note.md`
7. `docs/ops/notion-public-entry.md`
8. `docs/ops/personal-display-and-web.md`

如果要动终端，先看 Notion「终端」页顶部车道面板，沿自己的车道继续，不要直接写旧槽。

## 明天最小可做的三刀

### 1. 小红书 / 公众号平台包，不做自动外发

目标不是发出去，而是把发布包进一步变成可人工检查的平台草稿。

建议文件：

- `scripts/lib/platform-packages/xiaohongshu.mjs`
- `scripts/lib/platform-packages/wechat.mjs`
- 或先只写设计文档：`docs/ops/platform-package-next.md`

边界：

- 只生成草稿 / 图文包 / 封面字 / 标签。
- 不自动外发。
- 不把平台反馈写回 `events.jsonl`。
- `needsHumanApproval` 默认保持 `true`。

### 2. `palm` 命名与存储债，先收束文档残留

勘察结论：代码已迁移到 `成为/手心/current.md`；`palm.rs` 与 `sense.rs` 已同步。

`palm` 存储债已收束：代码与文档口径均指向 `成为/手心/current.md`。后续只继续观察 `palm` 作为动词命令是否站得住。

下一步可以做只读复核：