# 沃壤 Index · current

> 这不是全仓目录；这是 AI 冷启动第一跳索引。

## 1. 先读什么

- `README.md`：仓库总入口与空间判准。
- `AGENTS.md` / `CLAUDE.md`：代理冷启动与施工口径。
- `破土.md`：空间总规与冷启动入口。
- `docs/ops/worang-index/current.md`：本索引第一跳。

## 2. 根部必要入口

- `成为.md` → `成为/`
- `领域.md` → `领域/`
- `资源.md` → `资源/`
- `技能.md` → `技能/`
- `破土.md` → 空间总规

> 根部只列必要入口；完整根目录事实见 `current.json` 与 `docs/ops/root-manifest.json`。

## 3. 三立基第一跳

- `成为/`：运行态与成为地层。
- `领域/`：能力域、作品域、健康域与责任区。
- `资源/`：外部材料、参考、补给、跟练与旧文明残片。

## 4. 其他地层第一跳

- `技能/`：研究、捕捉、知录、审计等技能规则。
- `wo/`：动作接口、本地协议与 source pack。
- `docs/ops/`：机器运行、审计、发布、连接、对账与操作文档。
- `docs/cockpit/`：cockpit / 户口 / Project 产物层；branch-only 产物不一定在 main。
- `docs/sedimentation/`：workflow 沉淀产物；不是 Notion 镜像。
- `docs/legacy/`：退役历史参考；不作当前入口。

## 5. 机器发现的异常

- broken internal link: 100
- large source warning: 6

## 6. workflow 自动维护范围

- 可自动更新：`current.json`、`current.md`、可确定目标的相对链接。
- 只报警不自动改：语义入口、根目录白名单、Notion 链接、外部 URL、需要谷判断的归位。

## 7. 不要做什么

- 不把本页扩成全仓目录。
- 不把根部展开成白名单复刻。
- 不手动同步 `current.json`。
- 不把 Notion 复刻回 GitHub。
- 不重建 `docs/ideas/`；想法胚回 `成为/灵感/`，工具设计回 `wo/`。
