# 成为/项目 清理迁移记录｜2026-06-13

## 目的

`成为/项目` 不再作为项目容器、项目入口或项目判断层存在。

本轮清理的目标不是重建一个新项目总表，而是把旧 `项目` 父容器拆掉：

- 行动户口 / 控制权 / 晚上裁决：回 GitHub Project 1 / cockpit issue。
- 能力生长线：回 `领域/代码`、`领域/黑客`、`领域/量化` 等领域层。
- 作品与外显材料：回 `领域/作品` 或 `docs/ops` 发布 / 展示操作层。
- 已完成、已归档、历史材料：回沉积或操作证据，不再冒充当前项目。

## 本轮第一刀

先清掉最会误导后续判断的旧入口：

1. 备份旧 `成为/项目.csv` 到本文件同目录下的 `becoming-projects-ledger-retired-2026-06-13.csv`。
2. 删除原 `成为/项目.csv`。
3. 删除由旧项目表生成的 NotebookLM 项目包。
4. 后续再分批处理 `成为/项目/` 下的正文页与子目录。

## 已归位批次

- 旧 `成为/项目.csv`：备份为 `docs/ops/becoming-projects-ledger-retired-2026-06-13.csv`，原表删除。
- 旧 NLM 项目包：删除 `wo/nlm/becoming-projects-overview.md` 与相关 active 入口，`wo/nlm/manifest.json` 改为 retired pack 记录。
- `make`：停止从旧项目 CSV 显示项目清单，改为提示 GitHub Project 1 / cockpit issue。
- 代码 / 黑客 / 学习推进材料：归入 `领域/代码/学习推进记录/`、`领域/黑客/学习推进记录/` 等真实能力生长面。
- 量化、健康、研究、招聘、AI 协作语境等材料：归入对应领域、`资源/语境/` 或 `docs/ops/`。
- 自媒体与品牌材料：归入 `领域/作品/彼岸此地·集/`，旧 `成为/项目/推·自媒体/` 清空。
- 猜意、招聘、Coding Plan、AI 编程失败机制等研究材料：归入 `资源/语境/` 或 `领域/代码/学习推进记录/`。
- 论文审阅项目：仅保留沉积索引 `成为/沉积/论文审阅项目.md` 与 `成为/沉积/论文审阅项目/README.md`；旧 `成为/项目/论文审阅项目/` 正文与图片全部删除，完整历史留在 Git 提交层。

## 完成状态

截至本分支最新校验：

- `成为/项目.csv` 不存在。
- `wo/nlm/becoming-projects-overview.md` 不存在。
- `成为/项目/` 不存在。
- `成为/项目/论文审阅项目/` 不存在。
- 旧项目表相关文档口径已改为退休 / GitHub Project 1 / cockpit issue。

## 不做

- 不新建另一个“项目总览”。
- 不在 `成为/项目` 内留下 README 或归档页。
- 不把 GitHub Project 1 的行动户口复制回 Notion / 成为层。
- 不把所有材料粗暴塞进一个新的 archive 项目目录。

## 判断

清理后的空间里，`项目` 不再是父类。旧材料要按真实受力面归位，而不是继续被“项目”这个旧分类吸住。
