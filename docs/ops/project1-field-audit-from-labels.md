# 巡检仪（兼容入口）

> 旧名：字段审计 / field audit / Project 1 字段审计。  
> 新名：巡检仪 / status-phase-lock-inspector。

本文件只保留兼容入口，正文已迁移到：

```text
docs/ops/status-phase-lock-inspector-from-labels.md
```

脚本旧入口仍可用：

```bash
bash scripts/audit-project1-fields-from-labels.sh
```

但真实实现已转到：

```bash
bash scripts/status-phase-lock-inspector-from-labels.sh
```
