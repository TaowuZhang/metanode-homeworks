# 浏览器链接治理第二轮｜2026-07-05

本页记录一次性迁移、分类、清理与对账过程，因此居留在 `docs/ops/`，不放入资源候选目录。

## 范围

597 条旧链接完成静态分流：

- 原始 Chrome HTML 与 Safari ZIP 保存历史；
- 活动浏览器只保留当前真实入口；
- URL 候选回到 `资源/链接/`；
- 已形成使用语境的浏览器分工与工具架进入 `资源/语境/`；
- 执行、清理和对账证据留在 `docs/ops/`。

## 当前分流

- 39 条：已存在于 `资源/链接.csv`；
- 37 条：`资源/链接/浏览器主题包候选.md`；
- 17 条工具：常驻使用语境在 `资源/语境/浏览器工具架.md`；
- 48 条订阅候选：`资源/链接/浏览器订阅候选-2026-07-17.md`；
- 96 条项目与学习入口：`资源/链接/浏览器项目与学习入口候选.md`；
- 43 条账号与服务入口：`资源/链接/浏览器账号与服务入口.md`；
- 71 条待复核与一次性入口：`资源/链接/浏览器待复核与一次性入口.md`；
- 2 条垂直检索候选：`资源/链接/浏览器垂直检索候选.md`；
- 244 条历史、重复或搜索冗余：只留原始导出。

鸠摩搜索属于 71 条待复核与一次性入口中的后续复核项，不在垂直检索文件重复计数。

浏览器导出形成的文件是 cohort 索引，不替代已有的 `资源/链接.csv` 与单条 `资源/链接/*.md`；同名或同 URL 已有正式记录时，继续复用既有 canonical 条目。

## 浏览器执行结果

- Chrome 书签栏只剩 `工具架`、Photopea 与 Apifox；
- Zen 已导入 8 个工具；
- Safari 用户书签、阅读列表和自定义主题文件夹均为 0。

详细可见界面核验见：

- `docs/ops/browser-bookmark-cleanup-2026-07-05.md`

## 边界

- 候选 URL 不冒充正式资源；
- 账号入口不保存凭证或会话状态；
- 链接层不替项目、领域和语境层做迁居决定；
- Folo / TopHub 实际复查等待 2026-07-17。
